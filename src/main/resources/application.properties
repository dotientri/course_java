spring.application.name=demo
server.port=${PORT:8080} # Render s? t? cung c?p bi?n PORT, n?u không có thì m?c ??nh là 8080
server.servlet.context-path=/api

# =======================================
# DATABASE CONFIGURATION (S?A L?I)
# =======================================
spring.datasource.url=${SPRING_DATASOURCE_URL} # S? l?y t? Render
spring.datasource.username=${SPRING_DATASOURCE_USERNAME} # S? l?y t? Render
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD} # S? l?y t? Render
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
# Dùng 'validate' ho?c 'update' cho production, không dùng 'create-drop'
spring.jpa.hibernate.ddl-auto=create-drop

# =======================================
# JWT SECRET KEY (S?A L?I)
# =======================================
jwt.signerKey=${JWT_SIGNER_KEY}

# =======================================
# EMAIL CONFIGURATION (S?A L?I)
# =======================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dotientri0285@gmail.com # Gi? nguyên ho?c dùng bi?n môi tr??ng
spring.mail.password=${SPRING_MAIL_PASSWORD} # B?T BU?C dùng bi?n môi tr??ng
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
app.frontend.url=${APP_FRONTEND_URL} # S? là URL c?a frontend trên Render

# =======================================
# GOOGLE SIGN-IN (S?A L?I)
# =======================================
google.oauth2.client-id=${GOOGLE_OAUTH2_CLIENT_ID}

# =======================================
# VNPAY CONFIGURATION (S?A L?I)
# =======================================
payment.vnpay.url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
payment.vnpay.tmn-code=${PAYMENT_VNPAY_TMN_CODE}
payment.vnpay.hash-secret=${PAYMENT_VNPAY_HASH_SECRET}
payment.vnpay.version=2.1.0
# Các URL này s? ???c c?u hình l?i sau khi có URL th?t t? Render
payment.vnpay.backend-return-url=${PAYMENT_VNPAY_BACKEND_RETURN_URL}
payment.vnpay.frontend-return-url=${APP_FRONTEND_URL}/payment-result

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB